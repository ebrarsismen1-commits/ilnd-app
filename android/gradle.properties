org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Düşük RAM'li makineler için düşürüldü — build biraz yavaşlar ama artık
# Android Studio + emülatörle aynı anda çalışabilir. Bol RAM'li bir
# makinede daha hızlı build istersen -Xmx4G'ye çıkarabilirsin.
org.gradle.parallel=false
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
