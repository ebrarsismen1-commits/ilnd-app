# Decision Log (append-only)

Her faz/iş kapanışında bir satır. Büyük kararlar ayrıca ADR alır (docs/adr/).
Format: tarih · karar · neden · alternatif · risk/ders.

| Tarih | Karar | Neden | Alternatif | Risk / Ders |
|---|---|---|---|---|
| 2026-07-02 | Marka paleti yeşil/turuncuya restore edildi | Kod, CLAUDE.md marka tanımından sapmıştı (mor/pembe) | Mor paleti resmileştirmek | Ders: tasarım token'ları tek kaynaktan; sapma = regresyon |
| 2026-07-03 | Auth hataları enum kod + UI-l10n mimarisi | Provider'da hardcoded metin EN kullanıcıya TR hata gösteriyordu | Provider'a l10n taşımak | Ders: katman sınırı — metin yalnız UI'da |
| 2026-07-03 | AI hafızası uid-scoped storage + legacy migration | Hesaplar arası gizlilik sızıntısı | Logout'ta silme | Migration ile mevcut kullanıcı hafızası korundu |
| 2026-07-04 | Kullanıcı provider'ları köprü-auth'u da izler | İlk girişte Firestore stream'leri ölü kalıyordu | Stream retry sarmalayıcı | Ders: Firestore stream hatadan sonra kendini yenilemez |
| 2026-07-04 | LLM/analiz HTTP çağrılarına 60sn timeout | Asılı istek sohbeti kalıcı kilitliyordu | Cancel butonu | Kural #4 doğdu |
| 2026-07-04 | AppTheme.dark + themeMode bağlandı | Koyu modda Material overlay'ler açık kalıyordu | Overlay'leri tek tek boyamak | Kural #7 doğdu |
| 2026-07-04 | minify/shrink release'te KAPALI bırakıldı | Cihaz smoke testi yapılamadan runtime kırılma riski | Açıp proguard kuralları | Aktivasyon = cihaz testli ayrı iş |
| 2026-07-04 | Skill tabanlı workflow sistemi kuruldu | Öğrenilen desenler oturumlar arası kayboluyordu | Tek büyük CLAUDE.md | Çekirdek ince + skill'ler talep-üzerine (context ekonomisi) |
| 2026-07-04 | feature/analytics ayrı skill YAPILMADI | build ile çakışma; bağımsız iş akışı yok | 12 ayrı skill | İlke #10: kullanılmayan süreç borçtur |
| 2026-07-04 | Nav v2 kodlandı: merkez halka + Topluluk sekmesi; NOW blokerleri beklerken NEXT öne çekildi | Owner kararı (tasarım momentum'u) | NOW'u beklemek | Takip tam birleşmesi ve gerçek etkinlik listesi (arch:L) sonraki fazlarda |
| 2026-07-04 | Etkinlik+RSVP: kök events + rsvps/{uid} alt koleksiyonu (ADR-0002) | Idempotent RSVP, aggregate count, festival ölçeği | rsvpIds array | Yazma çekişmesi/1MB sınırı elendi |
| 2026-07-07 | Sohbet ekranı v2: halka başlıkta, ILND balonsuz+serif | Vizyon mockup'ı zaten bunu tasarlamıştı, sadece uygulandı | Balonu korumak | Marka jesti (halka) artık 2. ekranda da yaşıyor |
| 2026-07-07 | Keşfet v2: emoji 'stories' şeridi öldürüldü → Ritüeller rafı (gerçek hedefli) | Vizyon: 'Instagram klişesi, dokunulduğunda derinliği yok' — 5/6 kart zaten ölü dokunmaydı | Emoji şeridini korumak | Eşleşen içerik yoksa kart hiç gösterilmez (asla sahte özellik) |
| 2026-07-07 | Sen v2 (serif hero + halka ikon) + Takip'e geri butonu eklendi | Bug: Takip nav v2'de sekmeden push-route'a geçmişti ama header'da geri butonu unutulmuştu — iOS'ta çıkış yolu yoktu | Test yazmak (Firestore mock maliyeti orantısız) | Statik/veri-bağımsız UI eklemesi için manuel kod incelemesi yeterli görüldü |
| 2026-07-07 | Onboarding: 2 dosyada ölü buton-rengi override'ı + yanlış yorum kaldırıldı | Phase 7'de AppTheme.dark bağlanınca bu manuel kod gereksizleşti ama yorum güncellenmemişti | Yorumu güncelleyip kodu bırakmak | Aynı sonuç, daha az kod — refactor değil, stale-doc fix'i |
| 2026-07-07 | Paywall v2: halka + serif fayda başlıkları eklendi, 'kulüp üyeliği' çerçevesi BİLİNÇLİ ertelendi | Vizyonun üyelik çerçevesi (etkinlik erken erişimi, merch) henüz gerçek mekanizma değil | Vaat edip sonra inşa etmek | Ritüeller ilkesiyle aynı: asla sahte özellik göstermeyiz — roadmap LATER'da doğru yeri var |
| 2026-07-07 | Faz F: Journal/Referral/VibeCard/Ekle/Legal'de v2 değişikliği YAPILMADI | Tarama zaten token/geri-navigasyon açısından temiz çıktı — zorla iş üretmemek ilke #10 | Kozmetik değişiklik icat etmek | Bulunan tek şey: routeJournal ölü sabiti (Journal aslında MaterialPageRoute ile açılıyor), temizlendi |
| 2026-07-07 | BreathRing davranış testi eklendi ('appte çalışıyor mu' sorusuna kanıt) | Kod-varlığı yeterli kanıt değil; ölçek gerçekten değişiyor mu + döngü kapanıyor mu test edildi | Sadece findsOneWidget assert'i | Gerçek animasyon davranışını (statik değil) kilitliyor |
| 2026-07-07 | android/gradle.properties bellek talebi 8G+4G'den 1.5G+512M'ye düşürüldü | Kullanıcının düşük RAM'li makinesinde emülatör açılamıyordu — Gradle tek başına 12GB rezerve ediyordu | Kullanıcıya makine değiştirmesini söylemek | Build biraz yavaşlar, ama artık düşük RAM'li makinede çalışabilir; bol RAM'li makinede -Xmx4G'ye geri çıkarılabilir |
| 2026-07-07 | Faz 1 (Web Raporu): onboarding/profil kalıcılığı Supabase profiles'a taşındı (ADR-0003) | Cihaz-yerel bayraklar yüzünden web/yeni cihazda kayıtlı kullanıcı tekrar onboarding'e düşüyordu ('her seferinde baştan') | Minimal (yalnız bayrak backfill) veya Firestore users/{uid} | Şema panelde elle migrate edilmeli (docs/db/profiles_onboarding.sql — ship listesine girer); girişte hidratlama penceresi (splash) eklendi |
| 2026-07-07 | Faz 3 (Web Raporu): ana sayfa yeniden düzeni — tek merkezli ILND + Takip ana sayfaya + 'Bugünkü Niyet' kaldırıldı | Owner geri bildirimi: üstteki '+' ILND-merkezli olmalı, Takip Ayarlar'da değil ana sayfada, niyet bölümü istenmiyor | +'yı korumak / Takip'i iki yerde tutmak | Ekle-aksiyonları artık ILND yüzeyinin altında; dailyIntention provider+home_provider.dart silindi; orphan niyet l10n anahtarları kaldı (trivial) |
| 2026-07-07 | Faz 4 (Web Raporu): profil fotoğrafı Firestore users/{uid} base64; vibe card toplaması alt-sorgu bazında dayanıklı | Madde 8 foto yok; madde 7 paylaş 'hata/bir şey olmuyor' (muhtemelen deploy edilmemiş index tüm kartı çökertiyordu) | Supabase Storage (ekstra panel kurulumu) / share_plus web fallback | Foto Storage paketi gerektirmedi; vibe card artık bir metrik patlasa da üretilir. Madde 7 web-özgü paylaş-sheet sorunuysa Faz 2'de ele alınır |
| 2026-07-07 | Faz 5 (Web Raporu): ilk-giriş serbest-metin günlüğü → AI-tahminli 'neye ihtiyacın var?' şık seçici; şık → ILND sohbeti seed | Owner: prompt 'bugün nasılsın?' mood ile aynı+saçma; yerine şıklı, API'ye bağlı düşük-token tahmin istendi | Sadece prompt metnini değiştirmek / serbest-metni korumak | Yeni IlndService.suggestNeeds (quick tier, JSON, fallback şıklar); chat'e seedMessage (route extra); orphan firstEntryPrompt/Hint/Save/Ready l10n kaldı (trivial) |
| 2026-07-15 | Takip: sahte '4.2k adım' kartı kaldırıldı → gerçek alışkanlık sayacı; GERÇEK adım entegrasyonu (health paketi, Health Connect/HealthKit) Play Store hazırlığına paketlendi | Owner kararı: adım verisi web'de imkânsız, APK testi yerine mağaza çıkışıyla birlikte yapılacak | Şimdi APK ile denemek | Ritüeller ilkesi: asla sahte veri göstermeyiz; mağaza fazı açıldığında health paketi + izinler + stepsTodayProvider planı hazır |
| 2026-07-08 | AI zinciri canlıya alındı: functions/.env (SUPABASE_URL) + compute SA'ya Token Creator rolü + Firebase Auth init (Anonim) — üç eksik üç katmanda giriş akışını kırıyordu | Kullanıcı 'Claude API çalışmıyor' bildirdi; loglar mintFirebaseToken 500 → signBlob denied → CONFIGURATION_NOT_FOUND zincirini gösterdi | Tek tek semptom yamamak | Kurulum adımları yeni ortamda tekrarlanmalı (bkz. functions/.env.example) |
| 2026-07-17 | Faz 1 (Gelişim Planı): serviceAccountKey gitignore joker desene çevrildi (çift-uzantılı kopya silindi) + yarım kalan makale-i18n işi Kapı'dan geçirilip commit'lendi | Çalışma ağacında korumasız secret kopyası (P0) ve askıda kalmış tamamlanmış iş vardı | Kopyayı elle ignore listesine eklemek | Ders: secret ignore desenleri tek dosya adına değil joker desene yazılır |
| 2026-07-08 | quick tier claude-haiku-4-5 → claude-sonnet-4-6 (~3x maliyet, owner onaylı) + assistant-prefill 3 çağrıdan kaldırıldı (4.6+ 400 döndürür) + yemek ekranı dart:io File → XFile.readAsBytes (web fix) | Owner: 'Türkçesi bozuk, yemek yükleme çalışmıyor' — Haiku Türkçesi zayıf; prefill yüzünden yemek analizi hiç çalışmamıştı | Haiku'da kalıp prompt'la oynamak | Sert Kurallar #8-#9 eklendi; JSON ayıklama tek noktada (ai_json.dart, 6 test) |
| 2026-07-19 | Faz 1A (Büyüme): reminder altyapısı UI'ya bağlandı — profil toggle+saat, home'da 7-günlük pencere sync'i, tek seferlik davet kartı; flutter_local_notifications v22 API uyumu | Servis+provider ölü koddu (hiçbir ekran çağırmıyordu); izin, değer görüldükten sonra bağlam içinde istensin istendi | İlk açılışta sistem izin dialoğu | Davet cevabı kalıcı (soru tekrarlanmaz); bildirim opt-in ve günde 1 — non-preachy ilkesi. Cihazda uçtan uca bildirim doğrulaması bekliyor |
| 2026-07-19 | Faz 1B (Büyüme): karşılamada garantili geri-referans — son hafıza notu greet görevine birebir gömülür, değinme şart koşulur | 'Seni hatırlıyorum' anı modelin insafına kalmıştı; ürünün en kritik farklılaştırıcısı şansa bırakılamaz | Model çıktısını çalışma zamanında denetlemek (maliyet) | Görev-seviyesi zorlama yeterli sayıldı; canlıda gözlemlenecek. Gelecek: son not yerine 'en anlamlı not' seçimi |
| 2026-07-19 | Faz 1C (Büyüme): ilk vibe card onboarding çıkışına bağlandı — yığın home < kart < sohbet (atlayan da kartı görür) | İlk 'paylaşılabilir an' kaçıyordu; yeni durum eklemeden saf navigasyonla çözüldü | first_entry'den önce kart göstermek / kalıcı bayrak | Kart sohbetin altında beklerken verisini yükler; yeni kullanıcının sıfır-durumu 'sessiz hafta' metniyle karşılanır (ilk-gün varyantı Faz 3 stiline ertelendi) |
| 2026-07-19 | Faz 3a (Viral): vibe card görseline davet kodu rozeti + paylaşım metnine kod | Referral sistemi vardı ama paylaşım anına bağlı değildi — kartı gören kişinin gelme yolu kopuktu | Yalnız metinde kod (görselsiz) | Kod boşsa rozet çizilmez; testin ortaya çıkardığı stat-pill taşması Expanded'la sınıf-genelinde kapatıldı |
| 2026-07-19 | Faz 3b (Viral): quote card — ILND cümlesinin altında 'Karta çevir' → 9:16 alıntı kartı (/quote-card) | Kullanıcılar AI sohbet ekran görüntüsünü zaten paylaşıyor; markasız/kodsuz ziyan oluyordu | Uzun basışla menü (keşfedilebilirlik düşük) | 280 karakter kırpma + esnek blok: kart hiçbir içerikle taşmaz; alıntı seçimi kullanıcıda (otomatik 'vurucu cümle' tespiti yapılmadı) |
| 2026-07-19 | Faz 3c (Viral): streak eşik kartı (7/30/100) + yakala-paylaş shareCardCapture'a, kimlik+kod satırı CardFooter'a ortaklandı | Streak en gurur üreten veriydi ama paylaşılamıyordu; paylaşım kodu 3. kez kopyalanmak üzereydi | Eşikte otomatik popup (baskı üretir) | 7 gün altında kart yok — erken paylaşım gurur değil baskı üretir; giriş noktası sessiz (banner dokunuşu) |
| 2026-07-27 | Ücretsiz katman kotası cihazdan hesaba taşındı: sayaç SharedPreferences yerine sunucuda (`ai_usage/{uid}_{hafta}`, anthropicProxy yazar), istemci yalnız okur | Kullanıcı web ile mobil arasında geçince — ve depolamayı temizleyince/uygulamayı silip kurunca — haftalık limit sıfırlanıyordu: her cihaz kendi kotasını sıfırdan başlatıyordu | Yerel sayacı kullanıcı dokümanına yazmak (istemci yazabildiği için sıfırlanabilir kalırdı) | Premium muafiyeti de sunucudan çözülür (referral ödülü + RevenueCat REST). `REVENUECAT_SECRET_KEY` girilmeden mağaza aboneliği canlıya alınırsa gerçek aboneler kotaya takılır — DEPLOYMENT.md'ye yazıldı. İstemci `Purchases.logIn(uid)` ile aboneliği hesaba bağlar |
| 2026-07-30 | Hareket programı altyapısı: `movement_programs` koleksiyonu + Keşfet rafı + Firebase Storage/video_player oynatıcı (ADR-0004) | Owner isteği: video-hareket-programı desteği içerik gelmeden hazır olsun. Ürün bugün yalnız kod-içi 8 metin önerisine sahipti (movement_pool) | 5. sekme (nav v2'yi bozardı) · YouTube embed (marka dışı oynatıcı) · programları `articles`'a sıkıştırmak | Roadmap'te video/koç LATER'daydı, owner öne çekti. İçerik boşken raf hiç çizilmez (test kilitli). `REVENUECAT_SECRET_KEY` gibi burada da bir açık kalem var: kurallar deploy edilmeden koleksiyon prod'da ölü |
| 2026-07-31 | E-posta linkleri: `authDeepLinkRedirect` tek kaynak + Android intent-filter + iOS CFBundleURLSchemes; auth stream'ine onError | Şifre sıfırlama ve onay linkleri localhost'a düşüyordu (Supabase, allowlist'te olmayan `redirect_to`'yu sessizce Site URL'e çeviriyor) ve mobilde deep link hiç tanımlı değildi | Universal/App Links (imza parmak izi + Apple Team ID gerekiyor, elde yok) | Sert Kural #15 doğdu. Panel tarafı (Site URL + Redirect URLs allowlist'i, `com.ilnd.app://login-callback` dahil) owner'da; kod tek başına yetmez |
| 2026-07-31 | Türkçe kalite geçişi: ILND prompt'una 4 maddelik Türkçe dil kuralı + 461 .arb dizesinin editoryal denetimi (22 dize düzeltildi) | Owner 'Türkçe anlamsız şeylere çıkıyor, ör. hoş geldin geri' bildirdi. Metin .arb'de değildi: dil kuralı tek satırdı ('Türkçe konuşursun') ve model İngilizce kurduğu cümleyi Türkçe kelimelerle diziyordu | 461 dizeyi baştan yazmak (iyi metinleri bozma riski, kök nedeni çözmez) | Asıl kusur AI prompt'undaydı, .arb büyük ölçüde sağlamdı. Tekrar eden desen: her yeni özellik bloğu (hareket, trio, auth) kendi üslubunu getirmiş — konvansiyonlar artık testte kodlu. Prompt'un canlı etkisi cihazda doğrulanmalı |
| 2026-08-19 | Palet lavantadan krem+sage+amber'e geri döndürüldü | Owner mor paleti beğenmedi; DESIGN_SYSTEM zaten eski yeşil paleti belgeliyordu (lavanta commit'inde güncellenmemişti) | Lavantayı korumak / paleti yeniden icat etmek | Kod ile doküman tekrar tutarlı. Android splash renkleri de eşitlendi — açılışta renk sıçraması vardı |
| 2026-08-19 | CI'daki üç iş de düzeltildi: emülatör JDK 21, Android build için google-services yer tutucusu, Gradle heap | Temmuz'dan beri HİÇBİR koşu yeşil değildi; firebase-tools JDK 21 istiyor, google-services.json hiçbir işte üretilmiyordu, release build'i de aynı nedenle paket üretemezdi | CI'ı kırmızı bırakıp görmezden gelmek | İlk tam yeşil koşu. release.yml artık secret olmadan erken düşer; ci.yml fork PR'ları için yer tutucuya düşer |
| 2026-08-19 | Tasarım handoff'u birebir uygulandı: tipografi Noto Serif+DM Sans+IBM Plex Mono'ya geri, 10 rollük ölçek kilidi kaldırıldı | Owner kararı: handoff birebir. Handoff main'e bakarak yazılmıştı, dalın 4 gün önceki Sora/Inter geçişini görmemişti | Mevcut ölçeği korumak / ölçeği handoff ritmine göre yeniden ayarlamak | Kilit yerine iki zayıf koruma: aile kilidi + belgelenmiş punto kümesi. 2026-08-14'teki 'tek ölçek' düzeltmesi bilinçli geri alındı |
| 2026-08-19 | 12 ekran editoryal dile çevrildi: kutular kalktı, ayrım hairline+boşlukla, her ekranda tek büyük an | Handoff §7 'Editoryal Yasalar'; ekranlar kart yığınıydı ve hangi sayıya bakılacağını söylemiyordu | Yalnız renk/punto değiştirip yapıyı korumak | Takip'te donut → tek büyük sayı; Keşfet'te 400px kapak ekranın ilk şeyi; Sen'de üç kart tek şerit |
| 2026-08-19 | Takip Bugün'den çıkarıldı, tekrar kendi ekranı oldu (d18f43e geri alındı) | Owner: 'takip ekranı ana ekranda olmayacak, tamamen ekranları kopyalamanı istemiştim'. İlk turda README'yi okuyup prototip HTML'ini açmamıştım | Bugün'ün içinde tutmak (önceki karar) | İki kapı bırakıldı (Bugün'de sessiz satır + Sen'de ayarlar). Test artık 'gömülü değil ama ulaşılabilir'i kilitliyor — bu yüzey geçmişte iki kez kaybolmuştu |
| 2026-08-19 | Adan: ilerleme sayı yerine yer — öğe kazanımı sunucu-otoritatif (ADR-0006) | Handoff §7 + Sert Kural #13: kullanıcının yazabildiği yerde ödül yoktur | İstemcide hesaplayıp Firestore'a yazmak (kota hatasının aynısı) · her yazımda Firestore trigger | Dört öğe kazanılabilir, ikisi kilitli görünür (gece ritüeli cihaz-yerel, RSVP indeks istiyor). Ada illüstrasyonu owner kararıyla ertelendi; AdanCanvas tek yerden değişecek |
| 2026-08-19 | Arka plandaki hareketli aura gradyanları kaldırıldı (AnimatedBackground silindi) | Owner: 'çok AI duruyor'. Prototipte de 19 ekranın zemini düz renk | Gradyanı yavaşlatmak / yalnız web'de kapatmak | Fotoğraf scrim'leri ve paylaşım kartı zemini korundu (işlevsel + handoff'ta var). Web'de her karede 3 tam ekran boyama işi gitti |
| 2026-08-19 | Emoji yasağı: 60 satırdaki emoji kaldırıldı, testle kilitlendi | Owner: 'emoji hastalığına son'. Üç ayrı kaynaktan geliyordu (ikon yerine, .arb kuyruğu, paylaşım metni) ve kimse toplamını görmüyordu | Yalnız ikonları değiştirip metinleri bırakmak | Mood glifleri (☾ ◍ ◐ ✦ ☁) emoji değil, korundu. Test yazılır yazılmaz bir kaçağı yakaladı (varyasyon seçicili kalem) |
| 2026-08-24 | Erişilebilirlik geçişi: gündüz paleti AA'ya çekildi (accent/amber/muted koyulaştı, danger+water palete girdi), hareket-azaltma 5 yüzeye daha bağlandı, sekmeler ekran okuyucuya "seçili" diyor | UI denetimi: beyaz metinli birincil buton 3.49:1 (AA sınırı 4.5), su çubuğu kendi rayına 1.21:1, 9 animasyonlu dosyanın 7'si `prefersReducedMotion`'ı hiç sormuyordu | Yalnız buton rengini yamamak · kontrastı gözle ayarlamak | Gece paleti zaten geçiyordu, hiç dokunulmadı, çünkü sorun tek modda ve gözle görünmüyordu. Kontrast artık testte (test/core/contrast_test.dart, alfalı gece yüzeyleri zemine bindirilerek ölçülür). Palet üçüncü kez sessizce kaymasın. Nefes egzersizinin kendi hareketi bilerek durdurulmadı, orada hareket içeriğin ta kendisi |
| 2026-08-24 | Web deploy komutu belgelendi ve doğrulama adımı eklendi (DEPLOYMENT.md) | `flutter build web --release` `--dart-define-from-file=.env` olmadan koşuldu: derleme temiz, analiz temiz, deploy başarılı, ama canlıda Supabase hiç kurulmadı ve her kullanıcı giriş ekranına düştü | Komutu akılda tutmak | AppConfig'in tüm alanları boş dizeye düşüyor, yani eksik yapılandırma hiçbir yerde hata üretmiyor, yalnız çalışma zamanında sessizce ölüyor. Deploy öncesi `grep -c "supabase.co" build/web/main.dart.js` şartı kondu. Aynı taramada `.env`'de RECAPTCHA_SITE_KEY ve REVENUECAT_API_KEY'in hiç olmadığı görüldü (App Check web'de, paywall her yerde etkisiz) |
| 2026-08-26 | Ada illüstrasyonu çizildi: topografik yön (su eş yükselti halkaları / kıyı konturu / çizgi öğeler), altı öğe sprite'ı, `IslandPainter` | ADR-0006 §4'te görsel ertelenmişti; ekranda düz bir yüzey duruyordu ve handoff §7'nin "üç katman" isteği karşılanmamıştı. Üç yön mockup'ı gösterildi, owner topografik olanı seçti | Kesik kâğıt (düz siluetler, en ucuz) · alacakaranlık (degrade + ışık, kural #12'ye en yakın) · PNG/SVG asset seti | Asset yok, animasyon yok: hepsi `Path`, karanlık mod ayrı varyant istemiyor. Çizgi kalınlıkları hedef ölçeğe bölünüyor, yani dar telefonda incelmiyor (topografik yönün bilinen riski). Kilitli öğe kesik çizgili boşluk olarak duruyor, yerler sabit. Adan ekranındaki yüzey prototipe döndürülüp tam genişlik + köşesiz yapıldı (sapma notu ADR'de) |
| 2026-08-26 | Su artık sessiz günlerde koyulaşıyor: `island/{uid}.lastActiveDate` sunucuda hesaplanıp yazılıyor, istemci farkı alıyor | Handoff §7'nin tek davranış kuralının ("sessiz geçen günler suyu koyulaştırır, cezalandırmaz") kodda karşılığı yoktu | Sessiz gün SAYISINI yazmak (iki senkron arasında bayatlar) · istemcide hesaplamak (kural #13'ün sınıfı) · yalnız check-in'e bakmak | Öğün ve gece ritüeli de son etkin günü ilerletiyor: yalnız check-in'e bakan hesap, her gün yemeğini yazan ama günlük tutmayan kullanıcının suyunu haksız yere koyulaştırırdı. Kademe üç tane, dördüncüsü yok. Ölçüm iki gerçek hata yakaladı: su halkaları zemine karşı 1.43:1'di (görünmüyorlardı) ve ay `water` tokenıyla çizildiği için koyulaşan zeminle 2.99:1'e düşüyordu; ay mürekkep rengine alındı |
| 2026-08-31 | Yemek analizi ve paywall testle kapatıldı; AI analiz mantığı `FoodAnalyzer`'a ayrıldı | İki yüzey de testsizdi: biri çağrı başına en pahalı AI yüzeyi, diğeri gelirin tek kapısı. HTTP dalı widget'ın içinde olduğu için 429/bozuk JSON/zaman aşımı yolları hiç doğrulanamıyordu | Yalnız widget testi yazıp ağ dalını açık bırakmak · `http.Client`'ı provider'dan okumak (yetmiyor: `isAnthropicProxyConfigured` testte zaten false) | 18 yeni test. Hata kodları enum'a alındı (kural #1). Paywall'da gerçek bir taşma bulundu: alt satır 320px'te 269px taşıyordu, iki bağlantı Flexible'a alındı. RevenueCat statik olduğu için satın alma SONUÇ yolları hâlâ testsiz. `test/tmp_island_preview_test.dart` süiti kilitliyordu (üç saat), scratchpad'e taşındı |
| 2026-08-31 | Analitik olay sözlüğü yazıldı (docs/tr/ANALITIK_SOZLUGU.md) | 27 olay 10 dosyaya dağılmıştı ve yarısı `logEvent` kaçış kapısından serbest metinle gidiyordu; panelde bir olayın kaynağını bulmanın tek yolu grep'ti | Sözlüğü yayından sonra yazmak | Envanterdeki "11 olay" sayısı yanlıştı, gerçek 27. İki ölçüm hatası ortaya çıktı: `trio_*_done` toggle üzerinde atılıyor (işaret kaldırırken de sayıyor, tamamlama oranı şişik), `onboarding_step_completed` tek noktadan sabit değerle çağrılıyor (huni kurulamaz). En büyük boşluk: yemek analizi ve tüm gelir hunisi sıfır olay |
| 2026-08-31 | Dağıtım hedefi yalnız iOS; Google Play parkedildi | Kullanıcı kararı. Tek mağazaya odaklanmak iki inceleme süreci, iki imzalama zinciri ve iki test cihazını aynı anda taşımaktan ucuz | Web + iOS · Play'i sürdürmek · yayını tamamen ertelemek | Play'e ait hiçbir şey silinmedi, envanter `docs/arsiv-google-play/README.md`'de. iOS config zaten hazırdı (`GoogleService-Info.plist` gerçek); eksik olan Android'inki idi. Geliştirme makinesi Windows: iOS derlemesi bulut macOS koşucusu ister, ama Xcode'un ürettiği her ayar repo dosyası olduğu için Mac yalnız derleme için gerekli. Yayının önündeki asıl kapı Apple Developer üyeliği. Ayrıca görüldü: `.env`'de `REVENUECAT_API_KEY` hiç yok, yani paywall her derlemede sessizce etkisiz |
| 2026-08-31 | Yemek analizi ve gelir hunisi ölçüme bağlandı; AnalyticsService'e test kanalı ve sözlük bekçisi eklendi | Uygulamanın en pahalı AI yüzeyi ve gelirin tamamı sıfır olayla çalışıyordu; yayına çıkıldığında "ne işe yaradı" sorusu cevaplanamazdı. Ayrıca sözlük koda bağlı olmadığı için bayatlaması an meselesiydi | Olayları yayından sonra eklemek · sözlüğü elle güncel tutmak · AnalyticsService'i Riverpod'a taşımak (16 çağrı yerini kırardı) | 11 yeni tipli olay, 11 yeni test. `PaywallScreen.show` artık zorunlu `source` alıyor (6 çağrı yeri güncellendi): hangi duvarın ödemeye dönüştüğü ancak böyle okunur. `testSink` seam'i olmadan hiçbir olay iddiası kurulamıyordu, çünkü Firebase testte kurulu değil ve servis hatayı yutuyor. Bekçi testi iki yönlü çalışıyor ve kırmızı-yeşil doğrulandı: belgesiz olay da, kodda karşılığı olmayan sözlük satırı da kırıyor. Demo dalı da tamamlandı olayını atıyor, çünkü zaten kotayı düşüyordu. Satın alma SONUÇ olayları (completed/failed/restore) hâlâ testsiz: RevenueCat statik ve mağaza kanalı testte asılı kalıyor |
| 2026-08-31 | Gövde, etiket ve sayılar sistem fontuna (iOS: SF Pro) geçti; başlıklar Noto Serif kaldı | Owner: "SF Pro'ya geçelim ama başlıklar kalsın, ILND vuruculuğunu sadece başlıklar ve logo versin." Dağıtım yalnız iOS olunca yerli metin hissi ve Dynamic Type uyumu kazanılıyor, marka karakteri tek yerde toplanıp orada güçleniyor | Üç aileyi korumak (18 Ağustos handoff kararı) · her şeyi SF Pro yapmak (serif imza da giderdi) · SF Pro'yu asset olarak gömmek (Apple lisansı uygulama içi kullanımla sınırlı, sistem fontu zaten cihazda) | `fontFamily` bilerek boş bırakıldı: `.SF Pro Text` gibi nokta önekli adlar özel ve sürümle değişiyor, aile vermemek aynı sonucu güvenle veriyor. Sayılarda IBM Plex Mono'nun yerini `FontFeature.tabularFigures()` aldı; oradaki gereksinim baştan beri hizalamaydı, ailenin kendisi değil (gerekçe kodda yazılıydı). typography_test aile kilidi tek aileye indirildi, yani DM Sans veya Plex'e sessiz dönüş CI'da kırılır. AÇIK RİSK: Noto Serif hâlâ çalışma anında indiriliyor, yani markanın tek taşıyıcısı en kırılgan parça; ağsız ilk açılışta başlıklar sistem serifine düşer |
| 2026-08-31 | Noto Serif pakete gömüldü; lib/ artık hiç font indirmiyor | Gövde sisteme geçince başlıklar markanın TEK taşıyıcısı oldu, ama hâlâ google_fonts ile çalışma anında iniyordu: ağsız ilk açılışta ve App Store incelemesinde uygulama tam da kimliğini kaybederdi | Değişken fontu (1.8 MB, tüm alfabeler) gömmek · google_fonts'un asset önbelleğine güvenmek · riski kabul edip indirmeye devam | İki statik kesim, 400 ve 600, toplam 872 KB (SIL OFL). Serif yalnız w600'de kullanılıyor, 400 emniyet için. Tuzak: Google Fonts eski tarayıcı UA'sına TTF değil EOT döndürüyor ve Flutter geçersiz fontu SESSİZCE yok sayar; ilk indirme böyle bozuk geldi, bu yüzden test artık TrueType imzasını (0x00010000) da doğruluyor. Eksik dosyayı zaten Flutter'ın kendisi derlemede yakalıyor, bozuk dosyayı yalnız test yakalıyor. typography_test aile kilidi yerine indirme yasağına dönüştü |
| 2026-08-31 | Etkinlik kontenjanı İSTEMCİDE uygulanıyor; sunucu-otoritatif değil (bilinçli borç) | `capacity` etkinlik başına veride duruyordu ama hiçbir yerde kullanılmıyordu: 20 kişilik bir buluşma sınırsız RSVP kabul ediyordu. Firestore kuralları doküman SAYAMAZ, yani kapasite kuralda uygulanamıyor; `count()` aggregate'i kurallarda çalışmaz | Cloud Function + transaction (Sert Kural #13'ün istediği, `syncIslandItems` deseni) · sadece göstermek, engellememek · hiç dokunmamak | Eşzamanlı iki kayıt sınırı bir iki kişi aşabilir ve kararlı bir kullanıcı bilerek aşabilir. Kabul edildi çünkü aşılan şey para ya da veri değil, ücretsiz bir buluşmanın kişi sayısı; organizatör elle çözebilir. Kontenjan bağlayıcı olması gereken (ücretli, salon kapasiteli) bir etkinlik gelirse RSVP bir Cloud Function'a taşınmalı, karar o gün yeniden verilir. Yan bulgular: RSVP sonrası sayı yenilenmiyordu (`ref.invalidate` eklendi, yoksa kullanıcı katıldıktan sonra kontenjanı dolmamış görürdü) ve "Katıl"/"Geliyorum" Sert Kural #16'ya aykırı büyük harfliydi çünkü turkish_copy_test'in kilitli listesine hiç eklenmemişlerdi; düzeltilip kilide alındılar |
| 2026-08-31 | RevenueCat sunucu anahtarı `defineSecret`'e çekildi ve anthropicProxy'nin secrets listesine eklendi | `process.env.REVENUECAT_SECRET_KEY` Functions v2'de secret bildirilmediği sürece BOŞ gelir; `hasRevenueCatPremium` boş anahtarda sessizce false döner. Sonuç: parasını ödemiş abone ücretsiz katman kotasına takılır ve hiçbir yerde hata görünmez | Anahtarı functions/.env'e koymak (deploy'da taşınmaz) · riski kabul edip bırakmak | Aynı dosyadaki ANTHROPIC_API_KEY ve SUPABASE_SERVICE_ROLE_KEY zaten defineSecret kullanıyordu, yani desen vardı ve RevenueCat ona uymuyordu. `resolvePremium` yalnız anthropicProxy'den çağrıldığı için secret oraya bildirildi. Emülatörle fonksiyon testleri 55/55 yeşil. Etkili olması için `firebase functions:secrets:set REVENUECAT_SECRET_KEY` + functions deploy gerekir; anahtar girilene kadar davranış aynı (false), yalnız mekanizma doğru |
| 2026-08-31 | Paywall mağazayı doğrudan çağırmıyor: `BillingGateway` provider'ı eklendi | `RevenueCatService` statik olduğu için satın almanın SONUÇ yolları test edilemiyordu; testte `Purchases` yapılandırılmadığından çağrı mağaza kanalında asılı kalıyor, yani "başarılı satın alma premium açar", "iptal premium AÇMAZ", "hata nazik mesaja düşer" kurallarının hiçbiri doğrulanamıyordu | Tüm RevenueCatService'i provider'a taşımak (identify/forget/isPremium çağrı yerleri de değişirdi) · statik metotları mock'lamak (Dart'ta mümkün değil) · sonuç yollarını testsiz bırakmak | Arayüz bilerek dar: yalnız paywall'ın kullandığı iki işlem. `identify`/`forget`/`isPremium` hâlâ doğrudan servisten geçiyor. 4 yeni test (toplam 17): mağazanın üç cevabı (onay/ret/hata) ayrı ayrı kilitli, ve akış sürerken mağazaya TEK çağrı gittiği doğrulanıyor. Faz A'da eklenen purchase_completed / purchase_failed / restore_* olayları da böylece test kazandı |
| 2026-08-31 | Tipografi eski üçlüye döndü: Noto Serif + DM Sans + DM Mono, üçü de pakete gömülü | Owner aynı gün SF Pro denemesinden vazgeçti ("eski tipografiye dönelim"), sayılar için IBM Plex Mono yerine DM Mono'yu sordu. DM Mono, DM Sans'ın kendi monospace kardeşi olduğu için eşleşme daha doğal | Sayılarda IBM Plex Mono'yu korumak (bold'u var ama aile eşleşmesi zayıf) · sayıları DM Sans'la yazmak | ÖLÇÜLDÜ, tahmin edilmedi: DM Sans'ta `tnum` yok, yani sayıları onunla yazmak rakamları orantılı genişlikte bırakır ve sayaç satırı zıplatır; DM Mono'da da `tnum` yok ama gerekmiyor, monospace olduğu için hizalama tasarımdan geliyor. DM Mono 500'de bitiyor (600/700 kesimi üretilmemiş) ve sayılar 600/700 kullanıyordu; `mono()` artık ağırlığı kırpıyor ve `metric` açıkça 500 istiyor, çünkü kodun 700 deyip 500 çizmesi ileride kafa karıştırır. Bundle 872 KB'den 1.1 MB'ye çıktı (DM aileleri Latin altkümesi olduğu için küçük). Kırpma testle kilitli |
| 2026-08-31 | Bugünün okuması saatlik ve ilgi alanına duyarlı oldu (daily_read.dart) | Owner "her saat değişecekti, başlıklara göre değişecekti" dedi; araştırınca özelliğin HİÇ yazılmamış olduğu görüldü (kodda, tüm dallardaki git geçmişinde ve dokümanlarda yok). Asıl sorun başkaydı: seçim `kArticles[gün % 15]` idi, yani yalnız 15 maddelik ÇEVRİMDIŞI YEDEKTEN geliyordu ve içerik sprintlerinin Firestore'daki ~90 yazısı ana ekranda hiç görünmüyordu | Günlük bırakıp yalnız kütüphaneyi düzeltmek · Keşfet'in kategori filtresine bağlamak | Seçim saf bir fonksiyona alındı, 11 test. Kişiselleştirme FİLTRE DEĞİL sıralama tercihi: hedefe uyan yazı yoksa tüm kütüphaneye düşer, ekran boş kalmaz. Firestore sıra garantisi vermediği için havuz id'ye göre sıralanıp indeksleniyor, yoksa aynı saatte farklı yazı çıkardı. Hedef anahtarlarının onboarding'dekilerle aynı kaldığı testle kilitli: sapma olursa kişiselleştirme sessizce ölürdü |
| 2026-08-31 | Tercihler ekranı eklendi (ad, yaş, boy, kilo, aktivite, diyet, alerjiler) | Onboarding bu yedi alanı topluyordu ama hiçbirini sonradan düzenlemenin yolu yoktu. Alerji listesi tarif önerisini beslediği için bunun güvenlik tarafı da var: yanlış girilen alerji düzeltilemiyordu | Onboarding'i tekrar açtırmak · yalnız profil ekranında göstermek (düzenlemeden) | Yol boyunca GERÇEK BİR HATA çıktı: profil gerçekleri (`_profileFacts`) yalnız `_hydrate` içinde hafızaya yazılıyordu, o da sunucuda hazır profil varsa çalışır. Yani onboarding'i İLK cihazında dolduran kullanıcının boyunu kilosunu ILND hiç bilmiyordu, ancak ikinci cihazda giriş yaparsa öğreniyordu. `pushLocalProfile` ikisini birden yazacak şekilde ayrıldı. İkinci sorun: `addFact` yalnız birebir aynı dizeyi eliyor, yani kilo güncellenince eskisi de kalıyordu ve ILND iki kiloyu birden biliyordu; `replaceFacts` önek bazlı DEĞİŞTİRME yapıyor ve sohbetten öğrenilenlere dokunmuyor. Dar ekranda iki taşma bulundu ve düzeltildi (üç sayı alanı yan yana, ve 30 puntoluk "tercihler" başlığı geri okla birlikte) |
| 2026-08-31 | Keşfet: kapak dakikada bir dönüyor, liste kategorileri dönüşümlü diziyor ve ilgi alanını öne alıyor (explore_feed.dart) | Üç şikâyetin üçü de gerçekti: kapak `allArticles.first` idi (hep aynı yazı), liste ham Firestore sırasıydı ve içerik sprint sprint tohumlandığı için konu konu yığılıydı (15 meditasyon, sonra 12 egzersiz), kişiselleştirme hiç yoktu | Rastgele karıştırma · yalnız kapağı döndürüp listeye dokunmamak · listeyi kullanıcının kategorisine FİLTRELEMEK | Karıştırma RASTGELE DEĞİL, round-robin: her turda her kategoriden bir yazı. Rastgele olsaydı her yeniden çizimde liste değişir, kullanıcı yerini kaybederdi. Kova içi sıra da id'ye göre sabit, çünkü Firestore sıra garantisi vermiyor. Kişiselleştirme yine filtre değil sıralama: ilgi alanı öne gelir ama diğer konular listede kalır, yoksa keşfet ölürdü. Kapak ekrana her girişte ve dakika ilerledikçe döner; gözün önünde değişmesi için zamanlayıcı EKLENMEDİ, kartın okunurken kayması istenmedi. Yan etki: Keşfet artık SharedPreferences'a bağımlı, üç mevcut widget testine override eklendi ve filtre testinin "kapak listenin ilk yazısıdır" varsayımı düzeltildi |
| 2026-08-31 | Şifre sıfırlama linki `{{ .ConfirmationURL }}` yerine `{{ .TokenHash }}` akışına taşındı (uygulama `verifyOTP` ile token’ı kendisi tüketir); ayrıca çözülemeyen link artık sessiz kalmıyor ve kullanıcıyı onboarding duvarına değil giriş ekranına bırakıyor | Owner: “şifremi unuttuma basıyorum, linke tıklayınca şifre yenileme yerine giriş çıkıyor”. Link `?error=access_denied&error_code=otp_expired` ile dönüyordu: token Supabase’in `/auth/v1/verify` ucunda, uygulama hiç devreye girmeden tüketilmiş/ölmüş oluyordu. Üstelik hata yutuluyordu, çünkü `_onAuthStreamError` yalnızca `state is AuthPasswordRecovery` iken tepki veriyordu ve o duruma ancak takas BAŞARILI olunca geçiliyor | Yalnızca OTP süresini uzatmak (linki önden açan tarayıcıyı çözmez) · mobilde `app_links`’i doğrudan bağımlılık yapıp custom scheme’i token_hash ile sürdürmek (mail istemcileri custom scheme linklerini tıklanabilir yapmıyor) | Kod tek başına yetmez: panelde “Reset Password” şablonunun `{{ .TokenHash }}` linkine çevrilmesi şart, o yapılana kadar yeni yol ölü ve eski akış yürümeye devam eder (geriye dönük uyumlu). Recovery artık web’de tamamlanır; mobil kullanıcı linki tarayıcıda açıp yeni şifreyle uygulamaya girer. Hesap onayı linki ConfirmationURL + deep link yolunda bırakıldı, dokunulmadı |
| 2026-08-31 | Günlük girdisi ve alışkanlık silinebilir oldu (uzun basma + onay) | Kullanıcı yazdığı günlüğü ve eklediği alışkanlığı kaldıramıyordu. İki eksik farklı derinlikteydi: günlükte silme HİÇ yoktu (repository yalnız stream/add taşıyordu), alışkanlıkta `deleteHabit` yazılmış ve Firestore kuralı izin veriyordu ama metot hiçbir yerden çağrılmıyordu, yani ölü koddu | Kaydırarak silme (Dismissible) · satır sonunda çöp ikonu · onaysız silme | Uzun basma + onay penceresi seçildi: silme geri alınamıyor, tek dokunuşla olmamalı; alışkanlık satırında zaten kısa dokunuş tamamlama yapıyor, ikinci bir ikon satırı kalabalıklaştırırdı. Onay penceresi tek yerde toplandı (core/widgets/confirm_delete.dart). Günlük silinince check-in kaydı BİLEREK geri alınmıyor: kullanıcı o gün gerçekten yazdı, silmesi o günü yaşamamış saymaz, seri bozulmaz. 6 test. Test yazarken aynı tuzağa üçüncü kez düşüldü: hedef varsayılan viewport'un altında kalınca jest ekran dışına gidiyor ve sessizce hiçbir şey olmuyor, `ensureVisible` şart |
| 2026-08-31 | Sen ekranı: rozetler genişlik tavanına alındı, boş hafta grafik yerine cümle | Owner "burası çok çirkin duruyor" dedi. İki ayrı sebep vardı: dört rozet `Expanded` ile eşit paylaşıyordu ve masaüstü web'de her biri ~470px'lik boş kutuya dönüşüyordu (Adan yüzeyindeki hatanın aynı sınıfı, telefon düzeni gerilmiş); haftalık grafik ise boş haftada 96px yer ayırıp içini boş bırakıyordu (tüm çubuklar 4px, üstünde 86px hiçlik) ve ekran bozulmuş gibi duruyordu | Tüm ekrana genel bir max-width sarmalayıcı (her ekranı etkilerdi, tek başına karar verilemezdi) · rozetleri Wrap'e almak · boş haftada grafiği olduğu gibi bırakmak | Rozet genişliği LayoutBuilder ile hesaplanıp 120px tavanına alındı: dar ekranda dördü de sığıyor, geniş ekranda gerilmiyor. Boş hafta artık suçlandırmayan tek bir cümle (DESIGN_SYSTEM editoryal kuralı: "ara vermek de bakımın parçası" dili). 4 test; rozet tavanı kırmızı-yeşil doğrulandı. GENEL SORUN DURUYOR: telefon düzeni masaüstü webde geriliyor, bu üçüncü örnek (Adan, paywall alt satırı, rozetler). iOS-only hedefte yalnız önizlemede görünüyor ama inceleme oradan yapılıyor |
| 2026-09-02 | AI maliyet kademeleri (ADR-0007): model son çare, ilk çare yerel veri. Önce token muhasebesi kuruldu (`ai_token_usage`), tavanlar veriden sonra | Owner: "token kullanımı bizim için büyük sorun, premium bir kullanıcı bile 5 dolarlık token harcarsa zarara giriyoruz". `ai_usage` yalnız ÇAĞRI sayıyor: 20 kelimelik mesajla 20 bin karakterlik yapıştırma orada aynı "1 mesaj", faturada elli kat fark var. Günlük tavanı (quick 300, deep 60) sonuna kadar kullanan tek bir premium hesap günde 5 dolara ulaşabiliyor | Daha ucuz model (Haiku denenmiş, Türkçesi zayıf geldiği için geri alınmıştı, 2026-07-08) · yalnız tavanları indirmek (çok konuşan premium duvara çarpar) · kendi GPU'muzda model (bu hacimde sabit maliyet çok yüksek) · büyüyünce bakmak | Hiçbir tavan değiştirilmedi: owner "önce gerçek rakamı görelim" dedi, bir haftalık veri sonrası `npm run report:tokens` çıktısından karar verilecek. Muhasebe yanıtı bekletmiyor ve hata verse bile isteği bozmuyor. Aynı gün eklenen sohbet geçmişinin ortalama girdiyi artırdığı ADR'ye yazıldı |
| 2026-09-15 | Intelligence Layer V0 (ADR-0009): tek örüntü (P5), deterministik istatistik, kullanıcıya göstermeden önce önceden kayıtlı simülasyon kapısı | Bugünkü kişiselleştirme ölçülmeyen metin hafızası + Claude cümlesi; ilk gerçek çıkarımın rastgele veride yanlış örüntü üretmediği kanıtlanmalı | Beş örüntüyle başlamak; kişisel eşik; Claude ile yorum | Güç düşük, çoğu kullanıcıda örüntü çıkmaz; eşikler sonuca bakılarak değiştirilmez |
